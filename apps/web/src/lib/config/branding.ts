/**
 * Глобальная визуальная идентичность для общих UI-элементов.
 * Обновите эти значения при ребрендинге шаблона.
 *
 * Приоритет логотипа:
 * 1. logoLight / logoDark — отдельные логотипы для светлой и тёмной темы
 * 2. logo — единый логотип для обеих тем (если logoLight/logoDark не заданы)
 */
export const brandingConfig = {
	/** Универсальный логотип для обеих тем, если logoLight/logoDark не заданы. */
	logo: '',
	/** Логотип для светлой темы. Фолбэк на logoDark, затем logo. */
	logoLight: '/light_logo.png',
	/** Логотип для тёмной темы. Фолбэк на logoLight, затем logo. */
	logoDark: '/dark_logo.png',
	/** Человекочитаемое имя бренда для отображения в UI. */
	name: 'os-docs-v2'
};
